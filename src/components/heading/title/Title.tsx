import clsx from 'clsx';
import styles from '../../../styles/heading/title.module.css';

export const Title = () => {
  
    return (
        <h1 className={
            clsx(
            styles.title,
        )}>Miguel Rodríguez Pérez</h1>
    );
}